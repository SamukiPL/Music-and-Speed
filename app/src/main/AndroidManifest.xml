<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="me.samuki.musicandspeed">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".MusicApp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.main.MainActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.listcreation.ListCreationActivity"
            android:screenOrientation="portrait" />

        <service android:name=".services.media.CustomBrowserService">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>
        <!--<activity android:name=".SettingsActivity"-->
            <!--android:configChanges="orientation">-->
        <!--</activity>-->
        <!--<activity android:name=".NewListActivity"-->
            <!--android:configChanges="orientation">-->
        <!--</activity>-->
        <!--<service android:name=".MusicService"-->
            <!--android:exported="false" />-->
        <!--<activity android:name=".AudioListActivity"-->
            <!--android:configChanges="orientation">-->
        <!--</activity>-->
        <!--<activity android:name=".NewListActivityFragmented"-->
            <!--android:configChanges="orientation">-->
        <!--</activity>-->
    </application>

</manifest>
